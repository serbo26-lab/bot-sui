[Unit]
Description=Bot S-UI backup timer

[Timer]
# Production default: one lightweight local snapshot per day.
# Full migration packages are created manually when needed.
OnCalendar=*-*-* 03:15:00
RandomizedDelaySec=30min
Persistent=true

[Install]
WantedBy=timers.target
